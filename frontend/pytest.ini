[pytest]
testpaths = tests
python_files = test_*.py
base_url = http://localhost:5000
markers =
    modais: Testes de modais (editar, excluir, bulk). Use --run-modais para incluir.
    footer: Testes do footer de sessão (timer, nome, visibilidade).
